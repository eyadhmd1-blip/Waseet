source "https://rubygems.org"

# CocoaPods — iOS dependency manager required by expo prebuild
gem "cocoapods", "~> 1.16"

# xcpretty — formats xcodebuild output for readability in CI logs
gem "xcpretty", "~> 0.3"
